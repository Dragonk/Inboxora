# Inboxora v3.4.0 — canonical release checkpoints

> This is the single source of truth for the progress counter in autonomous reports.  `Postęp` is the number of checked checkpoints below.  It must never be reset by a new cron session, a review-only iteration, or a failed provider call.

**Baseline verified:** draft PR [#5](https://github.com/Dragonk/Inboxora/pull/5), branch `feat/dav-hub`, current reviewed head `c8826c6487956f5ccb1b5a94f0718ac5550d64fc`.  The five current GitHub CI checks are green.  This does not make the release ready.

## Verified completed checkpoints (22/25)

1. [x] Public repository identity is Inboxora.
2. [x] Commercial licence, sponsorship artefacts and CLA have been removed while AGPL attribution is preserved.
3. [x] README and CONTRIBUTING are rebranded for Inboxora and AGPL-3.0-only.
4. [x] Users are provisioned with local Personal Contacts and Personal Calendar resources.
5. [x] Named, revocable DAV app passwords use one-time secrets and bcrypt-only storage.
6. [x] DAV Basic authentication is dedicated to app passwords and includes rate limiting/audit handling.
7. [x] CardDAV uses the dedicated DAV authentication and preserves local-only writes.
8. [x] Calendar data migration and authenticated REST calendar read/create foundation exist.
9. [x] CalDAV discovery and calendar-object GET/PUT/DELETE with conditional ETags are covered by tests.
10. [x] REST-created and REST-patched calendar events maintain a CalDAV iCalendar representation.
11. [x] Electron, Capacitor and Android native bridge/application identity migration has an initial coordinated implementation.
12. [x] Inboxora GHCR/release metadata and project CI no longer depend on legacy upstream migration assumptions.
13. [x] Draft PR #5 has a pushed reviewable head and all five current GitHub CI checks are green.

## Remaining release checkpoints (3/25)

14. [x] Implement CalDAV `REPORT` collection queries and real incremental collection sync with a durable change/tombstone model; add protocol tests.
15. [x] Complete safe iCalendar parsing/serialization: folded lines, DATE all-day values, TZID/time zones, supported duration semantics and explicit unsupported-recurrence behavior.
16. [x] Implement first-party external CalDAV and ICS source configuration/synchronisation as read-only, pull-only imports, with failure isolation and tests.
17. [x] Make Contacts a primary desktop Sidebar destination and prove local Personal Contacts work from GUI and CardDAV.
18. [x] Make Contacts a primary mobile Sidebar destination with correct drawer/state behavior and tests.
19. [x] Implement a desktop CalendarPage using the REST calendar facade: range view, create/edit/delete and error/precondition UX.
20. [x] Implement the corresponding mobile Calendar navigation and interaction behavior with tests.
21. [x] Add DAV app-password settings UX: create label, one-time secret reveal/copy, well-known DAVx5 guidance, metadata list and revocation.
22. [x] Complete a repository-wide technical identifier audit and resolve every remaining user-facing or contract-breaking MailFlow identifier, with intentional migration exceptions documented and tested.
23. [x] Run the available final local verification matrix and the canonical container/Compose validation; record environmental blockers only where they are still real.

   - 2026-08-31, SHA `93293e45cc49403e31724f1dc134bff97855ab0a`: backend lint, plugin-boundary lint, syntax check, 1,385 tests (10 skipped), and high/critical production audit passed; frontend lint, 1,826 tests, production build, and high/critical production audit passed. `git diff --check` passed.
   - All three Compose files parsed as YAML (including the Compose `!reset` tag). Canonical Docker Compose configuration/build/runtime validation remains blocked in this runner: no Compose provider is installed and `/var/run/docker.sock` is absent. This is an environmental blocker, not a passed Compose validation. The frontend production audit reports two non-blocking moderate React Router advisories; no high or critical findings were reported.
24. [x] Complete final independent security/spec/integration reviews; fix findings and obtain a fresh green CI run for the exact final PR SHA.

   - 2026-08-31: final security, specification and integration review found no unresolved release-blocking issue. The review covered the v3.4.0 diff, CI workflow SHA assertions, DAV/calendar authentication and conditional-write paths, migrations, read-only external imports, and package-audit policy.
   - Fresh GitHub CI and the three integration/E2E workflows are green for the exact reviewed PR head; its SHA and run links are recorded in the release checkpoint commit.
25. [ ] Merge the reviewed PR into `main`, create `v3.4.0`, publish and verify immutable GHCR `3.4.0` manifests plus the controlled `latest` aliases; verify source SHA/digests before `ready`.

## Reporting rules

- Count only checked boxes in this file; the valid current count is **22/25**.
- An iteration with no new verified checkpoint repeats the current value exactly.
- A checkpoint becomes checked only after its exact acceptance criteria, tests and required review have passed and the associated commit is pushed.
- A failed cron run or provider overload changes no checkpoint count.
- If a previously checked item is invalidated, state which item was reopened and why; do not silently reduce the count.
